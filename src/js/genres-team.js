function getGenreById(ids, arrGanres) {
    let arrNamesGenres = [];

    for (const id of ids) {
        for (const genre of arrGanres) {
            if (genre.id === id) {
                arrNamesGenres.push(genre.name);
            }
        
        }
    }
}
function formatGenresName(array) {
    for (let i = 0; i <= array.length; i += 1)   {
            if(array[i].length)
        }
        arrNamesGenres.length
}